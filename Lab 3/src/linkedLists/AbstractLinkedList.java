package linkedLists; 

public abstract class AbstractLinkedList<E> implements LinkedList<E> {

	/**
	 * We cannot implement here the clone method because this is an abstract class, we would have to implement it in each LinkedList class.
	 */
}
